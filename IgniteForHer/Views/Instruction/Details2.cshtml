@model IgniteForHer.Models.Instruction

<div style="width:100%; height:100%; margin:auto;">
    <div class="col-md-4" style="height:600px; margin:auto; overflow-y: scroll;">
            @Html.Partial("Instruction2");
    </div>
    <div class="col-md-4" style="height:600px; margin:auto; border: none; background-color:black; color:white;">
            <h1>Code</h1>
        <textarea name="TextArea" rows="27" cols="46" style="margin-bottom:10px; border: none; background-color:black; color:white;">
            function ArrangeItems(area, item) {
            document.getElementById(area).src = item;
            }

            function ArrangeDecorations(item) {
            ArrangeItems("decor1", item);
            ArrangeItems("decor2", item);
            ArrangeItems("decor3", item);
            ArrangeItems("decor4", item);
            }

            function SetUpBanner(banner, year) {
            document.getElementById("year").innerText = year;
            document.getElementById("banner").innerText = banner;
            }

            ArrangeItems("cake", cake);
            ArrangeItems("people1", Dancers.top);
            ArrangeItems("people2", Dancers.middle);
            ArrangeItems("people3", Dancers.bottom);
            ArrangeItems("music", music);
            ArrangeDecorations(decorations);
            SetUpBanner(banner, year);

        </textarea>
        <form method="post" action="">
            <br />
            <input type="button" class="submit btn btn-primary btn-sm" id="btnpopup" value="Run Code" onclick="ShowModelPopUp();" />
            @*<input type="submit" value="run code" class="submit btn btn-primary btn-sm">*@
        </form>
        @*@{
            if (IsPost)
            {
                string textarea = Request["TextArea"];
                var total = "<script>" + textarea + "</script>";
                <p>
                    @Html.Raw(total) <br>
                </p>
            }
        }*@
    </div>
    <div class="col-md-4" style="height:600px; margin:auto; border:none">
        <h1>Result</h1>
        <img src="~/Content/IFHWithoutDJ.png" style="border:1px solid white; width:400px" />
    </div>
    </div>


<script type="text/javascript">
    ShowModelPopUp = function () {
        window.showModalDialog('/Instruction/Result2', "WindowPopup", "dialogHeight: 650px; dialogWidth: 800px;" +
                    "dialogTop: 50px;  dialogLeft: 150px; edge: Raised; center: Yes;" +
                    "help: No; resizable: No; status: No;");
}
</script>
